/**
 * =1, higher for testing
 */
const speedFactor = 1
/**
 * Work duration in seconds
 */
export const WORK_SECONDS = 25 * 60 / speedFactor;
/**
 * Break duration in seconds
 */
export const BREAK_SECONDS = 5 * 60 / speedFactor;
